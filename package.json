{
  "name": "node-demo",
  "version": "0.0.1",
  "scripts": {
    "test": "make test",
    "test-1": "make test-1",
    "test-2": "make test-2",
    "coveralls": "nyc --reporter=text-lcov npm run test | coveralls",
    "coveralls-dependencies": "nyc --reporter=text-lcov --exclude-node-modules=false npm run test | coveralls",
    "coveralls-1": "nyc --exclude-node-modules=false --reporter=text-lcov npm run test-1 | coveralls",
    "coveralls-2": "nyc --exclude-node-modules=false --reporter=text-lcov npm run test-2 | coveralls",
    "coverage-1": "nyc --exclude-node-modules=false --reporter=lcov npm run test-1",
    "coverage-2": "nyc --exclude-node-modules=false --reporter=lcov npm run test-2"
  },
  "devDependencies": {
    "coveralls": "3.1.0",
    "istanbul": "0.4.5",
    "jshint": "^2.12.0",
    "mocha": "^7.2.0",
    "mocha-lcov-reporter": "1.2.0",
    "nyc": "^15.1.0",
    "should": "9.0.2"
  },
  "engines": {
    "node": ">=0.8.6"
  },
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "dependencies": {
    "vulnerable-package": "github:nickmerwin/vulnerable-package"
  }
}
